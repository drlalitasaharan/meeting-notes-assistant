services:
  db:
    environment:
      POSTGRES_DB: meetings
      POSTGRES_USER: mna
      POSTGRES_PASSWORD: mna_local_pw

  backend:
    environment:
      DATABASE_URL: postgresql+psycopg2://mna:mna_local_pw@db/meetings
      REDIS_URL: redis://redis:6379/0

  worker:
    environment:
      DATABASE_URL: postgresql+psycopg2://mna:mna_local_pw@db/meetings
      REDIS_URL: redis://redis:6379/0
