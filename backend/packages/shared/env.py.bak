from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str = "postgresql+psycopg://postgres:postgres@localhost:5432/meetings"
    S3_ENDPOINT: str = "http://localhost:9000"
    S3_ACCESS_KEY: str = "minio"
    S3_SECRET_KEY: str = "minio123"
    S3_BUCKET_RAW: str = "meetings-raw"
    S3_BUCKET_DERIVED: str = "meetings-derived"

    OPENAI_API_KEY: str = "sk-..."
    TRANSCRIPTION_PROVIDER: str = "whisper"  # "assemblyai", "deepgram"
    SLACK_BOT_TOKEN: str | None = None
    NOTION_TOKEN: str | None = None
    NOTION_DB_ID: str | None = None

    class Config:
        env_file = ".env"

settings = Settings()
